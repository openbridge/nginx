# Allow all IPs using the catch-all IP address
allow 0.0.0.0/0;

# IPv6 equivalent (if needed)
allow ::/0;

# You can keep your specific IP allows for reference or future use
# allow 34.205.100.51;
# allow 192.168.0.0/16;
# allow 10.0.0.0/8;